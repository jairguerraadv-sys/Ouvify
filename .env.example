# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA - Ouvy SaaS
# =============================================================================
# Copie este arquivo para .env e preencha com valores reais
# NUNCA commit o arquivo .env no repositório!

# =============================================================================
# DJANGO CORE
# =============================================================================

# Chave secreta do Django (gere uma nova em produção)
# Para gerar: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=sua-chave-secreta-super-segura-aqui-min-50-caracteres

# Modo debug (True apenas em desenvolvimento)
DEBUG=True

# Hosts permitidos (separados por vírgula)
# Desenvolvimento: localhost,127.0.0.1,.local
# Produção: seu-dominio.com,www.seu-dominio.com
ALLOWED_HOSTS=localhost,127.0.0.1,.local,empresaa.local,empresab.local

# =============================================================================
# BANCO DE DADOS
# =============================================================================

# Tipo de banco: 'sqlite' ou 'postgresql'
DB_ENGINE=sqlite

# Configurações PostgreSQL (ignoradas se DB_ENGINE=sqlite)
DB_NAME=ouvy_db
DB_USER=postgres
DB_PASSWORD=sua_senha_postgresql
DB_HOST=localhost
DB_PORT=5432

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================

# Origens permitidas para o frontend (separadas por vírgula)
# Desenvolvimento: http://localhost:3000
# Produção: https://seu-dominio.com,https://www.seu-dominio.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Permitir credenciais (cookies) em requisições CORS
CORS_ALLOW_CREDENTIALS=True

# =============================================================================
# CONFIGURAÇÕES OPCIONAIS
# =============================================================================

# Idioma padrão
LANGUAGE_CODE=pt-br

# Timezone
TIME_ZONE=America/Sao_Paulo

# =============================================================================
# CLOUDINARY (Upload de Imagens)
# =============================================================================

# Credenciais do Cloudinary para upload de logo e favicon
# Obtenha em: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=seu-cloud-name
CLOUDINARY_API_KEY=sua-api-key
CLOUDINARY_API_SECRET=seu-api-secret

# =============================================================================
# NOTAS DE SEGURANÇA
# =============================================================================
# 
# 1. NUNCA compartilhe este arquivo preenchido publicamente
# 2. Em produção, defina DEBUG=False
# 3. Em produção, gere uma nova SECRET_KEY única
# 4. Configure ALLOWED_HOSTS com seus domínios reais
# 5. Use PostgreSQL em produção (não SQLite)
# 6. Configure CORS_ALLOWED_ORIGINS com as URLs reais do frontend
# 
# =============================================================================
