# =============================================================================
# OUVIFY SAAS - Monorepo Requirements (Wrapper)
# =============================================================================
# Source of truth lives in `apps/backend/requirements.txt`.
# This wrapper exists for repo-root tooling that expects `requirements.txt`.
# =============================================================================

-r apps/backend/requirements.txt
